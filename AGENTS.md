- 修改完代码后，不要编译，直接通知用户手动编译和测试

## 经验教训：编辑工具可能静默丢改动，改完必须读盘验证

本仓库多次发生同一事故（PotPlayer 命令、i18n 字符串、【自娱】一键高清命令均中招）：
SearchReplace/编辑工具报告 diff 成功，但个别改动实际未写入文件，且多数为同批次编辑中的单行插入。

- 高危场景：一次改动多个文件/多处时，其中某一行静默丢失（尤其是 switch/case 分发行、注册行、枚举/字符串表行）
- 典型症状：菜单项正常显示（菜单构建代码在），点击后 HandleCommand 的 switch 落空 → 无日志、无报错、无任何效果
- 规则：
  1. 每批编辑结束后，必须用 Grep/Read 重新读盘，逐条确认关键改动（case 行、ID 定义、i18n 项、函数声明/实现、注册/删除的行）确实落盘，不能只信工具的成功回执
  2. 遇到"UI 入口显示正常但点击后完全无反应"的问题，第一步先 Grep 验证 HandleCommand 对应 case 行是否存在，再排查其他环节